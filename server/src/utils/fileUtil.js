const fs = require('fs');
const path = require('path');

// handle json saving here

class fileUtils {
    static saveNewsToFile(data, fileName) {
        const filePath = path.join(__dirname, `${fileDate}.json`);
    }
}